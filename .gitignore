# Python bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/

# uv lockfile — this repo's reproducibility path is pip (see .github/workflows/invariants.yml);
# uv.lock is only generated as a side effect of running `uv run` locally and is not tracked.
uv.lock

# Packaging / build artifacts
build/
dist/

# Local environment / secrets (never commit real values)
.env
.env.*

# OS / editor cruft
.DS_Store
.vscode/
