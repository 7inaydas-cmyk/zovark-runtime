# F-003: Context Compaction Memory

State: draft architecture contract.

Context Compaction Memory prevents unbounded raw tool output from entering model
context. Phase 0 includes contracts and invariants only; no storage or retrieval
implementation exists.

